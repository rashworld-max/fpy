[fprime]
framework_path: ../../fprime-wasm
project_root: ../../..
; The fprime-wasm fork pins an older fpp than test/fprime; the shared tools
; from the harness dependency group work for both.
default_cmake_options: FPRIME_SKIP_TOOLS_VERSION_CHECK=ON
