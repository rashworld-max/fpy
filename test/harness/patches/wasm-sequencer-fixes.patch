diff --git a/Svc/WasmSequencer/WasmSequencer.hpp b/Svc/WasmSequencer/WasmSequencer.hpp
index 609c3eb..c47f9bc 100644
--- a/Svc/WasmSequencer/WasmSequencer.hpp
+++ b/Svc/WasmSequencer/WasmSequencer.hpp
@@ -985,6 +985,9 @@ class WasmSequencer final : public WasmSequencerComponentBase {
     //! Set up the fprime host interface
     void hostFprimeV1(spacewasm_host_t*);
 
+    //! Set up the "env" host module (float library calls)
+    void hostEnv(spacewasm_host_t*);
+
     /// FPrime Wasm Interface Host Functions
     spacewasm_hostcall_result_t wasmExit(struct spacewasm_caller_t* caller,
                                          const struct spacewasm_value_t* params,
diff --git a/Svc/WasmSequencer/WasmSequencerEvents.fppi b/Svc/WasmSequencer/WasmSequencerEvents.fppi
index 10d5628..d49e141 100644
--- a/Svc/WasmSequencer/WasmSequencerEvents.fppi
+++ b/Svc/WasmSequencer/WasmSequencerEvents.fppi
@@ -137,13 +137,6 @@ event SerialPortSendFailed(host_function: HostFunction, status: I32) \
   severity warning high \
   format "Host function {} failed to send on serial port: serialize status {}"
 
-@ The guest requested an event at a severity that is not permitted for guest
-@ programs (an out-of-range id, or FATAL/COMMAND which are reserved for the
-@ host). The guest message is preserved and reported here at WARNING_HI.
-event HostFunctionInvalidSeverity(raw: I32, msg: string size Wasm.GUEST_EVENT_MESSAGE_SIZE) \
-  severity warning high \
-  format "Guest event at invalid/forbidden severity '{}': {}"
-
 @ A sequence is starting after the main entrypoint was invoked
 event SequenceStarting(index: ModuleIdx) \
   severity activity high \
@@ -235,15 +228,17 @@ event InvalidModuleEntrypoint(moduleIdx: ModuleIdx, status: Status) \
   format "Module at index {} does not have a valid 'main' entrypoint: {}"
 
 # ----------------------------------------------------------------------
-# Guest-emitted events (fprime.event host function). One event per guest-
-# permitted F´ severity; the host function switches on the requested
-# severity id to emit the matching one so the event carries the correct
-# FPP severity. FATAL and COMMAND are intentionally NOT guest-emittable
-# (FATAL would let untrusted guest code trigger the FatalHandler; COMMAND
-# is reserved for the command dispatcher). A guest request for either, or
-# for an out-of-range id, is reported via HostFunctionInvalidSeverity.
+# Guest-emitted events (fprime.event host function). One event per F´
+# severity; the host function switches on the requested severity id to
+# emit the matching one so the event carries the correct FPP severity.
 # ----------------------------------------------------------------------
 
+@ A FATAL event emitted by the guest program
+event LogFatal(msg: string size Wasm.GUEST_EVENT_MESSAGE_SIZE) severity fatal format "{}"
+
+@ A COMMAND event emitted by the guest program
+event LogCommand(msg: string size Wasm.GUEST_EVENT_MESSAGE_SIZE) severity command format "{}"
+
 @ A WARNING_HI event emitted by the guest program
 event LogWarningHi(msg: string size Wasm.GUEST_EVENT_MESSAGE_SIZE) severity warning high format "{}"
 
diff --git a/Svc/WasmSequencer/WasmSequencerHelpers.cpp b/Svc/WasmSequencer/WasmSequencerHelpers.cpp
index 4a265b3..a91511f 100644
--- a/Svc/WasmSequencer/WasmSequencerHelpers.cpp
+++ b/Svc/WasmSequencer/WasmSequencerHelpers.cpp
@@ -105,10 +105,11 @@ void WasmSequencer ::createStore() {
     this->takeAllocatorLock();
 
     spacewasm_host_t host;
-    spacewasm_status_t status = spacewasm_host_new(1, &host);
+    spacewasm_status_t status = spacewasm_host_new(2, &host);
     FW_ASSERT(status == SPACEWASM_OK, status);
 
     this->hostFprimeV1(&host);
+    this->hostEnv(&host);
 
     spacewasm_compiler_options_t options;
     options.allow_memory_grow = false;
diff --git a/Svc/WasmSequencer/WasmSequencerHost.cpp b/Svc/WasmSequencer/WasmSequencerHost.cpp
index fd23420..d0082b9 100644
--- a/Svc/WasmSequencer/WasmSequencerHost.cpp
+++ b/Svc/WasmSequencer/WasmSequencerHost.cpp
@@ -4,6 +4,7 @@
 // \brief  cpp file for WasmSequencer Wasm Host functions
 // ======================================================================
 
+#include <cmath>
 #include <limits>
 
 #include "Fw/Types/Assert.hpp"
@@ -19,6 +20,52 @@
 
 namespace Svc {
 
+void WasmSequencer ::hostEnv(spacewasm_host_t* host) {
+    // The float library calls LLVM materializes for guest float arithmetic
+    // (llvm.pow, frem, llvm.log). wasm-ld leaves them under wasm's default
+    // import module "env". They are pure math, so they answer synchronously.
+    spacewasm_status_t status;
+    U32 module_idx;
+    status = spacewasm_add_host_module(host, "env", 3, 0, &module_idx);
+    FW_ASSERT(status == SPACEWASM_OK, status);
+
+    status = spacewasm_add_host_function(
+        host, module_idx, "pow", "dd", "d",
+        [](spacewasm_caller_t*, void*, const spacewasm_value_t* params, size_t n_params, spacewasm_value_t* out) {
+            FW_ASSERT(params != nullptr);
+            FW_ASSERT(n_params == 2, static_cast<FwAssertArgType>(n_params));
+            out->tag = SPACEWASM_F64;
+            out->u.f64_ = std::pow(params[0].u.f64_, params[1].u.f64_);
+            return SPACEWASM_CONTINUE_SOME;
+        },
+        nullptr);
+    FW_ASSERT(status == SPACEWASM_OK, status);
+
+    status = spacewasm_add_host_function(
+        host, module_idx, "fmod", "dd", "d",
+        [](spacewasm_caller_t*, void*, const spacewasm_value_t* params, size_t n_params, spacewasm_value_t* out) {
+            FW_ASSERT(params != nullptr);
+            FW_ASSERT(n_params == 2, static_cast<FwAssertArgType>(n_params));
+            out->tag = SPACEWASM_F64;
+            out->u.f64_ = std::fmod(params[0].u.f64_, params[1].u.f64_);
+            return SPACEWASM_CONTINUE_SOME;
+        },
+        nullptr);
+    FW_ASSERT(status == SPACEWASM_OK, status);
+
+    status = spacewasm_add_host_function(
+        host, module_idx, "log", "d", "d",
+        [](spacewasm_caller_t*, void*, const spacewasm_value_t* params, size_t n_params, spacewasm_value_t* out) {
+            FW_ASSERT(params != nullptr);
+            FW_ASSERT(n_params == 1, static_cast<FwAssertArgType>(n_params));
+            out->tag = SPACEWASM_F64;
+            out->u.f64_ = std::log(params[0].u.f64_);
+            return SPACEWASM_CONTINUE_SOME;
+        },
+        nullptr);
+    FW_ASSERT(status == SPACEWASM_OK, status);
+}
+
 void WasmSequencer ::hostFprimeV1(spacewasm_host_t* host) {
 #define HOST_FN(member_fn)                                                                                         \
     [](struct spacewasm_caller_t* caller, void* userdata, const struct spacewasm_value_t* params, size_t n_params, \
diff --git a/Svc/WasmSequencer/WasmSequencerInterpreter.cpp b/Svc/WasmSequencer/WasmSequencerInterpreter.cpp
index 6cf7579..9abf317 100644
--- a/Svc/WasmSequencer/WasmSequencerInterpreter.cpp
+++ b/Svc/WasmSequencer/WasmSequencerInterpreter.cpp
@@ -318,14 +318,16 @@ void WasmSequencer ::Svc_WasmSequencer_InterpreterStateMachine_action_dispatchPe
                     Svc::WasmSequencer_Status(static_cast<WasmSequencer_Status::T>(status)));
                 this->interpreter_sendSignal_hostResponseFailure();
             } else {
-                // Emit the event at the guest-requested severity. FATAL and
-                // COMMAND are forbidden for guest programs (FATAL would let
-                // untrusted code trigger the FatalHandler; COMMAND is reserved
-                // for the command dispatcher). A forbidden or out-of-range
-                // severity is reported via HostFunctionInvalidSeverity, carrying
-                // the raw id and the guest message, and the guest continues.
+                // Emit the event at the guest-requested severity. An
+                // out-of-range severity emits nothing and the guest continues.
                 const I32 rawSeverity = static_cast<I32>(this->m_pendingHostFunction.u.event.rawSeverity);
                 switch (static_cast<Fw::LogSeverity::T>(rawSeverity)) {
+                    case Fw::LogSeverity::FATAL:
+                        this->log_FATAL_LogFatal(msg);
+                        break;
+                    case Fw::LogSeverity::COMMAND:
+                        this->log_COMMAND_LogCommand(msg);
+                        break;
                     case Fw::LogSeverity::WARNING_HI:
                         this->log_WARNING_HI_LogWarningHi(msg);
                         break;
@@ -341,10 +343,7 @@ void WasmSequencer ::Svc_WasmSequencer_InterpreterStateMachine_action_dispatchPe
                     case Fw::LogSeverity::DIAGNOSTIC:
                         this->log_DIAGNOSTIC_LogDiagnostic(msg);
                         break;
-                    case Fw::LogSeverity::FATAL:
-                    case Fw::LogSeverity::COMMAND:
                     default:
-                        this->log_WARNING_HI_HostFunctionInvalidSeverity(rawSeverity, msg);
                         break;
                 }
 
